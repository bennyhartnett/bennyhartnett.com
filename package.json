{
  "name": "bennyhartnett-com",
  "version": "1.0.0",
  "description": "Personal website with uranium enrichment calculators",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
